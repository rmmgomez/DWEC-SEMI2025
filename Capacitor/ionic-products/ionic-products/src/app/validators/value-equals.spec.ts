import { ValueEquals } from './value-equals';

describe('ValueEquals', () => {
  it('should create an instance', () => {
    const directive = new ValueEquals();
    expect(directive).toBeTruthy();
  });
});
